rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 匿名認証済みであれば全コレクションへの読み書きを許可
    // （管理者ページが参加者全員のドキュメントを操作するため）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
